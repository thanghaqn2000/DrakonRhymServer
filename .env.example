# Copy this file to .env and fill in real values for local development.
# In production, set these directly via your deploy platform's env config
# (Docker --env-file, Render/Railway dashboard, Kubernetes secret, etc.).
# Do NOT commit .env — only this .env.example template.

# ---- Redis (audio cache to save YouTube bandwidth) ----
# A request for the same (video_id, pitch) within 24h is served from this
# cache instead of re-downloading from YouTube + re-running ffmpeg. Leave
# empty to disable; the server falls back to always-fetch behaviour.
# Example: redis://localhost:6379 (local) or redis://default:<pw>@<host>:<port>
REDIS_URL=
# Cache TTL in seconds (default 86400 = 24h).
DRAKON_CACHE_TTL_SECONDS=86400
# Max video duration in seconds — videos longer than this are rejected
# before download (default 420 = 7 minutes).
DRAKON_MAX_DURATION_SECONDS=420

# ---- yt-dlp YouTube auth ----
# Production server IPs are often challenged by YouTube with "Sign in to
# confirm you're not a bot". Export cookies from a real signed-in browser
# session in Netscape cookies.txt format, mount the file into the container,
# then point this env var to that in-container path.
# Example Docker path: /run/secrets/youtube-cookies.txt
DRAKON_YT_DLP_COOKIES_FILE=
# Local-only fallback if running on a machine with a browser profile available.
# Do not use this in slim Docker images unless the browser profile exists there.
# Example: chrome, firefox, edge
DRAKON_YT_DLP_COOKIES_FROM_BROWSER=
# JavaScript runtime used by yt-dlp to solve YouTube signature/n challenges.
# Docker image installs nodejs, so keep this as node unless you intentionally
# provide another supported runtime.
DRAKON_YT_DLP_JS_RUNTIME=node

# ---- Supabase (user persistence + per-user quota) ----
# Project URL from Supabase dashboard → Project Settings → API.
SUPABASE_URL=
# SERVICE ROLE key (not anon) — server-side only, bypasses Row Level Security.
# Keep this OUT of frontend bundles and OUT of any commit. Leave both empty to
# disable Supabase entirely; the server falls back to anonymous in-memory mode.
SUPABASE_KEY=

# ---- Google sign-in ----
# OAuth 2.0 Client ID from https://console.cloud.google.com → APIs & Services
# → Credentials → OAuth 2.0 Client IDs (type: Web application).
# This value is PUBLIC (frontend reads it), but kept in env for environment
# separation (dev/staging/prod usually use different client IDs because the
# Authorized JavaScript origins are different).
# Leave empty to disable auth checks entirely (DEV mode).
DRAKON_GOOGLE_CLIENT_ID=

# ---- Optional knobs (defaults shown) ----
# DRAKON_MAX_CONCURRENT=2
# DRAKON_YT_DLP_TIMEOUT=300
# DRAKON_FFMPEG_TIMEOUT=600
# DRAKON_RATE_LIMIT_PER_DAY=20    # per-user daily download cap
# DRAKON_ALLOWED_ORIGINS=http://localhost:8000,https://your-production-domain.com
#   ^^ Avoid `*` in production. The audio API is auth-gated, but a wildcard
#   origin still widens the attack surface — any site can read response bodies
#   and headers via cross-origin requests. Use an explicit list of origins.
#   Default in code is `*` (dev convenience); override in prod.
# PORT=8000

# IMPORTANT: the per-user daily quota is held in process memory only.
# - Process restart resets every user's counter to zero for the day.
# - Multiple workers (uvicorn --workers > 1, gunicorn pre-fork, multiple
#   container replicas) each keep their own counter, so the effective cap
#   becomes <workers> × RATE_LIMIT_PER_DAY. Run with --workers 1 until the
#   counter is persisted to a shared store (SQLite / Redis).
